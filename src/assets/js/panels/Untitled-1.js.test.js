 
       /*     this.connect = await connect
           // let pseudo = await this.getdate(connect.pseudo)
            async init(config, connect) {
                this.config = config
                this.connect = await connect
                this.database = await new database().init();
                this.initconnect();
                this.initLaunch();
                this.initStatusServer();
                this.initBtn();
            }
            async initconnect() {
                for (let connect of this.connect) {
                    let pseudo = await this.getdate(connect.pseudo)
            }}
            let connect = `http://54.154.229.228/PhenixBleu/json.php`;